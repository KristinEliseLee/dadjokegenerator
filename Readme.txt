Uses icanhasdadjokes.com to create markov chain genereated dad jokes.

Saves base jokes to a text file.
Saves original jokes to a different text file

To use:

$ pip install -r requirements.txt
$ python makejoke.py

To test:

$ pip install -r requirements.txt
$ python -m doctest -v makejoke.py
